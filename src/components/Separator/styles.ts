import {StyleSheet} from 'react-native';
import {opacityWhite80} from '../../theme/colors';

export default StyleSheet.create({
  separator: {
    flex: 1,
    borderBottomWidth: 1,
    borderColor: opacityWhite80,
    width: '95%',
  },
});
